# Visual OCR observation fixtures

These three cases consume the committed VISION-007 scale=2 synthetic crops and
bind the same-case VISION-008 result as sibling evidence. The inventory is the
only runtime admission authority.

The committed results are diagnostic observations, not OCR labels or accuracy
claims. Math and senior physics currently produce no observations; junior
physics preserves RapidOCR's raw `+++++++++` observation. Every result keeps
ground truth unavailable, acceptance not evaluated, human review required,
semantics not inferred, and Track integration absent.
