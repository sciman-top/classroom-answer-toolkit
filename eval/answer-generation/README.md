# Synthetic Answer Generation Fixtures

This evaluation surface contains only public, fully synthetic generation
requests. `tools/answer-generator` deterministically materializes the committed
candidate Markdown and `AnswerGenerationResult` artifacts without network
access or a model provider.

These fixtures prove provider-neutral contract plumbing and provenance binding.
They do not represent real model output, live gateway verification, workflow
integration, or live acceptance.
