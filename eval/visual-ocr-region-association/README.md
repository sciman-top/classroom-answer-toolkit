# Visual OCR-region Association Diagnostics

This evaluation surface contains three hash-bound request/result pairs, one
canonical inventory, and one deterministic report compiled from the committed
VISION-008/009/010 public synthetic authorities.

The current fixtures contain no honest positive association: math and senior
have no OCR observations, while the junior candidate/observation pair is
disjoint. The report therefore records two `unavailable` cases, one
`unmatched` case, and zero canonical matches. Positive and ambiguous geometry
exist only as policy-unit regressions and are not canonical samples or truth.

This diagnostic is not OCR correctness, a real association benchmark, layout
semantics, FigureUnderstanding, Track B, workflow integration, or live
acceptance.
