# Senior Physics Answer Sentinel Suite

本目录只证明 `senior-physics-answer` subject-pack 的独立 snapshot、两种 profile 和 validator 合同。
共享 Markdown、renderer、review、visual 与 delivery 合同由主产品包
`eval/junior-physics-answer/` 运行一次，不在这里机械复制。

保留的 sentinel：

- `smoke-answer`：classroom/compact validator sentinel。

运行入口仍为 Full verifier；该 sentinel 使用 Senior 自己的 manifest、规则、profile 和
compiled snapshot。
