# Visual Semantic Projection Diagnostics

This evaluation surface contains one public synthetic semantic declaration,
one hash-bound projection request/result pair, one canonical inventory, and one
deterministic report.

The admitted `junior-readable-measurement` case projects the explicitly
declared `measurement_reading` role only because VISION-011 maps
`truth-label-001` to `ocr-observation-001`, VISION-012 maps that truth label to
`text-region-001`, and VISION-014 binds those endpoints through
`ocr-region-association-001`. The recognized text `12` comes from the bound OCR
observation, not from truth text, geometry, confidence, file names, or subject
rules.

This diagnostic is not layout semantics, figure understanding, a problem
evidence bundle, Track B, answer generation, delivery trust, WPF integration,
eligibility, gateway live verification, or live acceptance.
