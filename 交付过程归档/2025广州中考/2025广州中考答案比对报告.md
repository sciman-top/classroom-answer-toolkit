# 2025 广州中考物理答案比对报告

## 结论

最短主链已经真实完成两次盲答调用，但两次候选均不能直接作为参考答案交付。
最终答案已依据原卷与官方参考答案逐题校正，并重新进入校验和 PDF 渲染流程。

后续对官方答案 PDF 的文本层和三页页图复核发现，早先人工校正稿把选择题第 8—10 题误写成 `D、D、D`，并且第 18 题的圈号小问发生了错位。官方页首的权威答案串为 `BAADA CBBDD`；正式交付已据此更新为 `BAADA / CBBDD`，第 17、18 题也已按官方页图重核。

## 运行基线

- 原卷：`广州物理中考试卷/2025广州中考.pdf`，共 8 页。
- 运行提示词：`prompts/junior-physics-answer/spec.md`，版本 v8.14。
- 提示词 SHA-256：`5971492f1175d56676e8085189b0a96fa0baeb0e085b9d4ebb3c5c4499b909d6`。
- 模型：primary provider，`gpt-5.5`。
- 参考答案：`广州物理中考试卷/2025广州中考（答案）.pdf`，只在盲答完成后用于比对。

## 第一轮盲答主要错误

- 第 8 题输出 C，参考答案为 B。
- 第 11 题把能量形式写成“风、太阳”，参考答案为“机械、光”。
- 第 17 题漏掉部分小问；第（3）问输出 `5 V`，参考答案为 `6 V`。
- 第 17（6）三个电流表示数均输出 `0.44 A`，参考答案均为 `0.5 A`。

## 第二轮盲答主要错误

- 选择题输出 `BAAAA CBDCC`，参考答案为 `BAADA CBBDD`。
- 第 11 题第二空输出“电”，参考答案为“光”。
- 第 12 题混入了平面镜作图内容，并把“小于”写成“大于”。
- 第 17（3）输出 `a、1.0 V`，参考答案为 `b、6 V`；三个电流表示数输出 `0.42 A`。
- 第 18 题的测量对象、平衡方向和两个浮力表达式均出现明显错误。

## 根因与修复

- v8.14 规范本身已覆盖读图、接线、仪表和小问边界，继续扩写 100 KB 主提示词不是主要解法。
- 运行任务段增加了逐题覆盖清单、视觉题独立复核、仪表量程/接线柱/分度值检查和概念术语检查。
- 两次结果的波动表明，单次整卷视觉请求仍存在注意力和小图分辨率风险；当前生产边界必须保留参考答案或人工复核。
- Windows Node 24 对中文交付路径上的 `fs.rmSync` 普通文件删除会原生崩溃；渲染临时文件和 manifest 锁文件已改用 `unlinkSync`，PDF 提交改为 `copyFileSync + unlinkSync`。

## 当前验收边界

- 已证明：真实原卷可以通过 v8.14 + AI 生成 Markdown，并完成校验、PDF、review 页图和 delivery manifest。
- 未证明：单次盲答可以无需复核直接可信交付。
- 当前可用流程：`原卷 -> AI 盲答候选 -> 参考答案/人工比对 -> 校正 Markdown -> PDF 交付`。

## 本次最终复核

- 复核模式：原卷 8 页 + 官方答案 3 页 + 同一官方 PDF 的文本层 + 盲答候选，`gpt-5.5` 返回 HTTP 200。
- 选择题页图与文本层均为 `BAADA CBBDD`，正式 Markdown 的两行选择题与其逐位一致。
- 官方答案第 17 题确认：`b`、`6 V`、猜想一，三个电流表均为 `0.5 A`；第 18 题确认依次为 `G`、右端、左端动力变小、从小桶内取出、`G_0`、`G-GL/L_0`。
- Markdown 已通过 subject-pack 校验；PDF、review 页图与 delivery manifest 已重新生成。该交付有 2 页；长中文行仅产生 3 条非阻断换行提示，视觉复核未见裁切、重叠或 LaTeX 泄漏。
