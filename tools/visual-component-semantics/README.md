# Visual component semantics

This deterministic public synthetic compiler binds an explicit fixture-author declaration to the
current `junior-instrument-scale` structure result and 2x crop. It groups one pointer edge pair and
five major-tick edge pairs. It does not infer a scale range, divisions, values, readings, question
binding, FigureUnderstanding, Track input, answers, trust, or live acceptance.

```powershell
npm --prefix tools/visual-component-semantics test
npm --prefix tools/visual-component-semantics run validate:fixtures
```
