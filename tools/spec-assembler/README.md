# Spec Assembler

将 `prompts/specs/assemblies/*.json` 装配为：

- `prompts/specs/compiled/*.md`
- `prompts/<subject-pack>/spec.md`

默认命令：

```powershell
npm --prefix tools/spec-assembler run assemble:all
```
